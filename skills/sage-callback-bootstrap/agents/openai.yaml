interface:
  display_name: "Sage Callback Bootstrap"
  short_description: "Restore or create Sage/Apollo callbacks."
  default_prompt: "Use $sage-callback-bootstrap after Mythic reset to import a baked Apollo callback config when available, create Sage, and verify live callbacks."
