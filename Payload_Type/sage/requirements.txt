langchain==1.2.13
langchain-core==1.2.31
langchain-mcp-adapters==0.2.2
# langchain-mcp-adapters 0.2.2 imports `mcp.server.fastmcp`, which mcp 2.0.0 removed, and it declares
# `mcp` with no upper bound — so a fresh resolve takes 2.0.0 and every module that reaches ai/mcp.py
# dies at import. Observed 2026-07-29 on a rebuilt venv: 24 collection errors, whole suite red.
# 1.25.0 is the version the green baseline was built against. Revisit when adapters gains mcp 2 support.
mcp==1.25.0
langchain-anthropic==1.4.0
langchain-aws==1.4.1
langchain-openai==1.1.12
# mythic 0.3.0rc5 (Mythic v4) pulls gql 4.0.0 + websockets 15 + aiohttp 3.14 transitively.
# langchain-google-genai stays disabled: google-genai caps websockets<15.1.0, which the v4 SDK line exceeds.
#langchain-google-genai==4.1.3
langchain-groq==1.1.2
langchain-ollama==1.0.1
langchain-xai==1.2.2
langgraph==1.1.3
langgraph-prebuilt==1.0.8
langgraph-checkpoint-sqlite==3.0.3
langsmith==0.7.22
mythic==0.3.0rc6
mythic-container==0.7.0rc9
# mythic-container 0.7.0rc1's generated gRPC stubs require grpcio>=1.78.0 (its metadata leaves grpcio
# unpinned). Pin both so the container build is deterministic and doesn't regress to 1.76.x.
grpcio==1.78.0
grpcio-tools==1.78.0
# Pinned for deterministic image builds — these are pulled transitively by mythic 0.3.0rc5 (Mythic v4);
# pinning to the versions the pytest baseline is green against avoids silent drift on rebuild.
gql==4.0.0
websockets==15.0.1
aiohttp==3.14.1
aiosqlite==0.22.1
arize-phoenix==13.17.0
arize-phoenix-evals==2.11.0
openinference-instrumentation-langchain==0.1.61
docker==7.1.0
ldap3==2.9.1
